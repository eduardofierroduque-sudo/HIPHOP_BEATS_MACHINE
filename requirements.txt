# --- Núcleo de Inteligencia Artificial ---
torch>=2.2.0
torchvision
torchaudio
audiocraft

# --- Procesamiento de Audio y Texto ---
av==11.0.0
julius
encodec
librosa
spacy
torchmetrics
demucs

# --- Interfaz Gráfica y Utilidades ---
customtkinter
scipy
omegaconf
hydra-core
huggingface_hub
transformers
sentencepiece

# 1. Herramientas base
pip install --upgrade pip setuptools wheel

# 2. Binarios de audio (Evita error de C++ en Windows)
pip install av --only-binary=:all:

# 3. Procesamiento de lenguaje
pip install spacy
python -m spacy download en_core_web_sm

# 4. Resto de dependencias
pip install -r requirements.txt
